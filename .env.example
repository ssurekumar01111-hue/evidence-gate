# DataHub GMS (General Metadata Service) REST/GraphQL URL
DATAHUB_GMS_URL=http://localhost:8080

# DataHub GMS Token (optional for local quickstart)
DATAHUB_GMS_TOKEN=

# DataHub MCP Server URL / endpoint (if connecting via remote MCP server)
DATAHUB_MCP_URL=http://localhost:8080

# Google Gemini API Key for LLM explanations and remediation patch drafting
GEMINI_API_KEY=
